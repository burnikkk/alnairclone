import { LoginCard } from '@/components/login/components/LoginCard';

export const Body = () => {
  return (
    <div className="relative w-svw h-[calc(100svh-56px)] bg-violet-light/10 flex items-center justify-center">
      <LoginCard />
    </div>
  );
};
